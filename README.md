# OSDomotics

## Repo Structure:

```text
code/
├── build.sh                  # Bash script for building, cleaning, and running the project
├── manual_interaction.sh     # Bash script to simulate physical actions on devices
├── src/                      # Folder containing C source code
│   ├── controller.c          # Source code for the main Controller shell
│   └── common.h              # Header file with shared constants and error codes
└── bin/                      # (Automatically generated) Folder containing executables
